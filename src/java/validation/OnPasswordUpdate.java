package validation;

import javax.validation.groups.Default;

/*
 * Dit is een validatiegroep die wordt gebruikt wanneer het paswoord is veranderd en opnieuw moet
 * worden gevalideerd.
 */
public interface OnPasswordUpdate extends Default
{
}
